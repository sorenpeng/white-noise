# Documentation Layout Rules

This repository keeps all non-code documents in the `docs/` directory.

Rules:

1. Product and technical plans stay in `docs/`.
2. Architecture notes stay in `docs/`.
3. Root only keeps project-level files like `README.md`, config files, and app directories.

